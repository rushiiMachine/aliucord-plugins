version = "1.0.1"
description = "Remove Discord's built-in slash commands."

aliucord {
    changelog.set(
        """
        # 1.0.1
        * Fix NPE

        # 1.0.0
		* Released
        """.trimIndent()
    )
}
