version = "1.0.3"
description = "Remove Discord's built-in slash commands."

aliucord {
	changelog.set(
		"""
            # 1.0.3
            * Fix changelog

            # 1.0.2
            * Update patches

            # 1.0.1
            * Fix NPE

            # 1.0.0
            * Released
        """.trimIndent()
	)
}
