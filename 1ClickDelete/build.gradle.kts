version = "1.0.1"
description = "Disable the delete confirmation on messages."

aliucord {
	changelog.set(
		"""
			# 1.0.1
			* Dismiss menu after click

			# 1.0.0
			* Released
		""".trimIndent()
	)
}
