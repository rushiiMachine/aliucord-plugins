version = "1.0.0"
description = "Show reply author, (optionally) show reply content, and fix clicking on them."

aliucord {
    changelog.set(
        """
        # 1.0.0
		* Released
        """.trimIndent()
    )
}
