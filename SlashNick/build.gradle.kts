version = "1.0.1"
description = "Bring the /nick command to mobile."

aliucord {
	changelog.set(
		"""
            # 1.0.1
            * Fix changelog

            # 1.0.0
            * Released
        """.trimIndent()
	)
}
