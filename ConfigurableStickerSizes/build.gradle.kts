version = "1.0.1"
description = "Makes sticker sizes configurable."

aliucord {
    changelog.set("""
        # 1.0.1
        * Fix text size
        
        # 1.0.0
		* Released
    """.trimIndent())
}
