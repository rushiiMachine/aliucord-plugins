version = "1.1.5"
description = "Makes sticker sizes configurable."

aliucord {
	changelog.set(
		"""
            # 1.1.5
            * Fix changelog

            # 1.1.4
            * Update to new Aliucord

            # 1.1.3
            * Update patches

            # 1.1.2
            * Update to 101.3

            # 1.1.1
            * Stop moving stickers up until proper bugfix

            # 1.1.0
            * Reset button to change to 240px
            * Move stickers upwards to fill gap

            # 1.0.1
            * Fix text size

            # 1.0.0
            * Released
        """.trimIndent()
	)
}
