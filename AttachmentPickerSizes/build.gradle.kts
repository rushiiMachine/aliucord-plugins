version = "1.0.1"
description = "Add upload sizes to the attachment picker."

aliucord {
	changelog.set(
		"""
			# 1.0.1
			* Bind existing size tags if recycled
			* Cache file sizes

			# 1.0.0
			* Released
		""".trimIndent()
	)
}
