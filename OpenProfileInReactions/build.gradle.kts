version = "1.0.1"
description = "Open users' profiles in reaction lists on click."

aliucord {
    changelog.set(
        """
        # 1.0.1
        * Uncached users sometimes don't show

        # 1.0.0
		* Released
        """.trimIndent()
    )
}
