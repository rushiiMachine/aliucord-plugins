version = "1.0.1"
description = "Opens the debug log as soon as opening the app & replaces the gift button"

aliucord {
    changelog.set(
        """
        # 1.0.1
        * Update patches

        # 1.0.0
		* Released
        """.trimIndent()
    )
}
