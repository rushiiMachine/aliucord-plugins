version = "1.2.3"
description = "Hide identifiable information."

aliucord {
    changelog.set(
        """
            # 1.2.3
            * Update to Discord 112104

            # 1.2.2
            * Fix reflection

            # 1.2.1
            * Fix changelog

            # 1.2.0
            * Hide AKAs
            * Hide user connections
            * Update to new Aliucord

            # 1.1.0
            * Toggleable parts of plugin

            # 1.0.1
            * Hide phone number on SMS 2fa element

            # 1.0.0
            * Released
        """.trimIndent()
    )
}
