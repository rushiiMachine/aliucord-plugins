version = "1.0.0"
description = "Play audio attachments directly in chat."

aliucord {
    changelog.set(
        """
            # 1.0.0
            * Released
        """.trimIndent()
    )
    author("Zt", 289556910426816513L)
}
