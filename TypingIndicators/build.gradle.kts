version = "1.0.0"
description = "Show typing indicators in the channel list."

aliucord {
    changelog.set(
        """
            # 1.0.0
            * Released
        """.trimIndent()
    )
    author("Zt", 289556910426816513L)
}
