version = "1.0.1"
description = "Export your friends list with /friendsexport"

aliucord {
	changelog.set(
		"""
			# 1.0.1
			* Escape markdown when printing in chat

            # 1.0.0
            * Released
        """.trimIndent()
	)
}
