version = "1.0.0"
description = "Custom gestures for certain elements. Check README for more information."

aliucord {
    excludeFromUpdaterJson.set(true)
    changelog.set(
        """
            # 1.0.0
            * Released
        """.trimIndent()
    )
}
