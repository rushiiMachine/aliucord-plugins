version = "1.1.0"
description = "Reverts uppercased text in various places."

aliucord {
    changelog.set(
        """
        # 1.1.0
        * Configurable header text size

        # 1.0.0
		* Released
        """.trimIndent()
    )
}
