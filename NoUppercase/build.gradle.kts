version = "1.2.0"
description = "Reverts uppercased text in various places."

aliucord {
    changelog.set(
        """
        # 1.2.0
        * Add invite cards

        # 1.1.0
        * Configurable header text size

        # 1.0.0
		* Released
        """.trimIndent()
    )
}
