version = "1.1.0"
description = "Replaces the TM and R emoji with one that's more visible."

aliucord {
    changelog.set(
        """
        # 1.1.0
        * Add Registered Trademark & Copyright emoji

        # 1.0.0
		* Released
        """.trimIndent()
    )
}
