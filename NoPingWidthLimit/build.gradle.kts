version = "1.1.0"
description = "Increase the max width of the ping count label."

aliucord {
    changelog.set(
        """
        # 1.1.0
        * Support text channel mention counter
        * Support channel mentions

        # 1.0.0
		* Released
        """.trimIndent()
    )
}
