version = "1.0.1"
description = "Show an servers/online friend count in your server list."

aliucord {
    changelog.set(
        """
        # 1.0.1
        * Remove a dev log

        # 1.0.0
		* Released
        """.trimIndent()
    )
}
