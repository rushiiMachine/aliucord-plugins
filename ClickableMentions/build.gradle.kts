version = "1.0.0"
description = "Enable clicking user/channel mentions everywhere. (ex. embeds/channel topics)"

aliucord {
    changelog.set(
        """
        # 1.0.0
		* Released
        """.trimIndent()
    )
}
