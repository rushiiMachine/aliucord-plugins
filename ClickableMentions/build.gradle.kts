version = "1.0.3"
description = "Enable clicking user/channel mentions everywhere. (ex. embeds/channel topics)"

aliucord {
    changelog.set(
        """
            # 1.0.3
            * Fix changelog

            # 1.0.2
            * Remove a dev log

            # 1.0.1
            * Fix random crash(?)

            # 1.0.0
            * Released
        """.trimIndent()
    )
}
