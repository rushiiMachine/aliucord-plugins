version = "1.0.1"
description = "Hides per-server user avatars everywhere."

aliucord {
    changelog.set(
        """
        # 1.0.1
        * Fix the plugin

        # 1.0.0
		* Released
        """.trimIndent()
    )
}
