version = "1.0.1"
description = "Queue up reactions to avoid hitting the ratelimit."

aliucord {
	changelog.set(
		"""
			# 1.0.1
			* Improve reflection
			* Use single threaded executor as queue

			# 1.0.0
			* Released
		""".trimIndent()
	)
}
