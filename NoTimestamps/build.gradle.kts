version = "1.0.1"
description = "Hide timestamps on messages."

aliucord {
    changelog.set(
        """
            # 1.0.1
            * Fix changelog

            # 1.0.0
            * Released
        """.trimIndent()
    )
}
