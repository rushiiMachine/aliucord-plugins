version = "1.0.1"
description = "Replaces every single piece of text with what you want. " +
	"YOU NEED TO REMEMBER HOW TO DISABLE THIS IN THE PLUGIN TAB BEFORE ENABLING IT."

aliucord {
	changelog.set(
		"""
            # 1.0.1
            * Fix changelog

            # 1.0.0
            * Released
        """.trimIndent()
	)
}
