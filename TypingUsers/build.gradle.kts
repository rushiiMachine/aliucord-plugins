version = "1.0.0"
description = "Opens a clickable list of all the currently typing users by pressing on the bar."

aliucord {
	excludeFromUpdaterJson.set(true)
	changelog.set(
		"""
			# 1.0.0
			* Released
		""".trimIndent()
	)
}
