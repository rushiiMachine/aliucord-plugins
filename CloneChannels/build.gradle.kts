version = "1.0.0"
description = "Quickly clone text channels in the context menu. (perms, name, topic, etc.)"

aliucord {
    changelog.set(
        """
            # 1.0.0
            * Released
        """.trimIndent()
    )
}
