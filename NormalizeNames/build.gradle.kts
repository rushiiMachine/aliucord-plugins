version = "1.0.0"
description = "Display nick/user names with fancy english chars reverted to normal."

aliucord {
    changelog.set(
        """
        # 1.0.0
		* Released
        """.trimIndent()
    )
}
